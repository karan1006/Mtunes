<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dell.mtunes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeScreen"
            android:label="HOMESCREEN" />
        <activity
            android:name=".RomanceGenreHomescreen"
            android:label="ROMANTIC TUNES" />
        <activity android:name=".PartyGenre"
            android:label="PARTY TUNES"/>
        <activity android:name=".BhangraGenre"
            android:label="BHANGRA TUNES"/>
        <activity android:name=".RoadGenre"
            android:label="ROAD TUNES"/>
            <activity android:name=".RomanticWindow"
                android:label="NOW PLAYING"/>

    </application>

</manifest>